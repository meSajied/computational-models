#include<bits/stdc++.h>
using namespace std;

inline int dda(x0, y0, x1, y1){

    int dx, dy, steps;
    float
    dx = x1 - x0;
    dy = y1 - y0;
    steps = abs(dx) > abs(dy) ? abs(dx) : abs(dy);

}